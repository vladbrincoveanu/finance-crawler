{
  "annotations": {"list": []},
  "editable": true,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "targets": [{"expr": "sum by (source, outcome) (rate(ingestion_rows_total[5m]))", "refId": "A"}],
      "title": "Ingestion rows / second",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "fieldConfig": {"defaults": {"unit": "percentunit"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "targets": [{"expr": "database_ready", "refId": "A"}],
      "title": "Database readiness",
      "type": "timeseries"
    },
    {
      "datasource": {"type": "prometheus", "uid": "Prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 24, "x": 0, "y": 8},
      "id": 3,
      "targets": [{"expr": "histogram_quantile(0.95, sum by (le, source) (rate(ingestion_run_duration_seconds_bucket[5m])))", "refId": "A"}],
      "title": "P95 ingestion duration",
      "type": "timeseries"
    }
  ],
  "schemaVersion": 39,
  "title": "VIC ingestion",
  "uid": "vic-ingestion",
  "version": 1
}
